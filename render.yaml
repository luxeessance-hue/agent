services:
  - type: web
    name: moroccan-law-tutor
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: OPENAI_API_KEY
        value: sk-RVApjtnPznKZ4UXosZYEOQ
      - key: OPENAI_BASE_URL
        value: https://toknroutertybot.tybotflow.com/
